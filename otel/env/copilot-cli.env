# GitHub Copilot CLI → OTLP collector.
#
# Verified against docs.github.com/en/copilot/reference/copilot-cli-reference
# and the CLI changelog on 2026-09-11. CLI runtime re-read 2026-09-21.

# Not strictly required: setting OTEL_EXPORTER_OTLP_ENDPOINT auto-enables OTel.
# Set it anyway so the intent is legible.
#
# That auto-enable is NOT a CLI convenience — it is the documented behaviour of
# the VS Code extension too, verified in the shipped 0.60.0 bundle. Anyone who
# exports OTEL_EXPORTER_OTLP_ENDPOINT for another harness turns Copilot on with
# it. See README trap 9.
COPILOT_OTEL_ENABLED=true

# --- READ THIS BEFORE SETTING AN ENDPOINT ---------------------------------
#
# The Copilot CLI is REPORTED to silently disable export to any http:// endpoint
# — including http://localhost:4318. It does not warn, and it does not abort
# startup. From `copilot help monitoring`: export is disabled "rather than sent
# in cleartext; startup is not aborted".
#
# The VS Code extension exports to that exact same endpoint without complaint.
# So a collector on localhost:4318 receives VS Code data and NOTHING from the
# CLI, with no error anywhere, and you conclude the CLI has no telemetry.
#
# Tracked: github/copilot-cli#4567 (open as of 2026-09-12, reported against
# v1.0.80).
#
# >>> UNREPRODUCED. The runtime installed on this machine is v1.0.54, which
# >>> predates v1.0.80, and in it the refusal is absent — no "cleartext" string,
# >>> no protocol check, and its own help text lists http://localhost:4318 as
# >>> the first worked example. Run `just copilot-smoke` to see what YOUR
# >>> runtime does before designing around this. See README trap 2.
#
# Two workarounds, pick one:
#
#   (a) terminate TLS in front of the collector and use https://
#       OTEL_EXPORTER_OTLP_ENDPOINT=https://collector.internal:4318
#
#   (b) use the file exporter and ingest the JSON-lines out of band
#
# > CAUTION on (b): the VS Code EXTENSION's file exporter writes the literal
# > string {} for every span — README trap 8. The CLI is a different runtime
# > with a different implementation, and its output format has NOT been
# > verified here. Check with `grep -c '^{}$'` before trusting a CLI dump.
COPILOT_OTEL_EXPORTER_TYPE=file
COPILOT_OTEL_FILE_EXPORTER_PATH=/private/tmp/copilot-otel/spans.jsonl

# Only otlp-http or file. There is no grpc on this surface — if you configure
# otlp-grpc in VS Code, the CLI runtime still uses HTTP.
#
# COPILOT_OTEL_EXPORTER_TYPE=otlp-http
# OTEL_EXPORTER_OTLP_ENDPOINT=https://collector.internal:4318

# Defaults to http/json. http/protobuf needs v1.0.61 or newer; on older builds
# this variable is silently ignored (github/copilot-cli#2934).
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf

OTEL_SERVICE_NAME=github-copilot

# Default false. On the VS Code side this gate is not reliably enforced —
# three open defects export content regardless.
OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=false

# Auth to the collector. Keep real tokens out of this file; `just leaks` scans
# *.env precisely because this is where one would end up.
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer REPLACE_ME
